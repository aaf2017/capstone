npm init -y
npm install body-parser cors express mongoose nodemon
npm install dotenv //+create .env file for storing env. variables (security)

//SET UP DATABASE + import to index.js
npm start //localhost:5000/posts

//stop server
npm install bcryptjs jsonwebtoken



//?git add .
//?git commit -m 'Initial commit'//stop server

//pushing the project to github - create new repository first (IMPORTANT: without README or .gitignore)!

git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/aaf2017/capstone7.git
git push -u origin main

//for existing:

git remote add origin https://github.com/aaf2017/capstone7.git
git branch -M main
git push -u origin main